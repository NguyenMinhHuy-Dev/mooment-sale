import { createSlice } from "@reduxjs/toolkit";

type ProductState = {

}

const initialState = {
    value: {

    } 
}

export const products = createSlice({
    name: "products",
    initialState,
    reducers: {

    }
});